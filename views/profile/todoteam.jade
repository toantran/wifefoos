- if ((typeof profile !== 'undefined') && (typeof user !== 'undefined') && (profile._id == user._id))
  - if (typeof profile.team == 'undefined' || !profile.team || !profile.team._id)
    .todo-noteam
      | You don't have a team yet
      .buttons
        input#createteambtn(type='button', value='Create your team', profileid='#{user._id}')
        input#jointeambtn(type='button', value='Find a team')        

